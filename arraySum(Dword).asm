DATA SEGMENT
    
    ARR DD 00123456H,0111F244H,34H
    TOTAL DD ?
    
DATA ENDS

CODE SEGMENT
    ASSUME CS:CODE DS: DATA
    
 START: 
 MOV AX,DATA
 MOV DS,AX
 
 MOV AX,0
 MOV DX,0
 MOV CX,03
 MOV SI,0
 
BEGIN:  

ADD AX,ARR[SI] 
ADC DX,ARR[SI+2]
ADD SI,4
LOOP BEGIN

MOV TOTAL,AX
MOV TOTAL+2, DX

CODE ENDS

END START