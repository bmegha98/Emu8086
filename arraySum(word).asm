DATA SEGMENT
    
    ARR DW 12H,67H,1234H,6754H
    TOTAL DW ?
    
DATA ENDS

CODE SEGMENT
    ASSUME CS:CODE DS: DATA
    
 START: 
 MOV AX,DATA
 MOV DS,AX
 
 MOV AX,0
 MOV CX,04
 MOV SI,AX
 
BEGIN:
ADD AX,ARR[SI]
ADD SI,1
LOOP BEGIN

MOV TOTAL,AX

CODE ENDS

END START